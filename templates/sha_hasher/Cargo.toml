[workspace]
members = ["host", "guest", "aggregation_guest"]
resolver = "2"

[workspace.dependencies]
byteorder = "1.5.0"
sha2 = "0.10.8"

# argumentcomputer/zisk fork: upstream v1.0.0-alpha + Blake3 compression precompile
zisk-sdk = { git = "https://github.com/argumentcomputer/zisk.git", branch = "blake3-precompile" }
ziskos = { git = "https://github.com/argumentcomputer/zisk.git", branch = "blake3-precompile" }
